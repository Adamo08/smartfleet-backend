webhooks:
  enabled: false

payments:
  paypal:
    enabled: false

email:
  provider: smtp

spring:
  mail:
    host: localhost
    port: 2525
    properties:
      mail:
        smtp:
          from: test@example.com
spring:
  main:
    allow-bean-definition-overriding: true
  datasource:
    url: jdbc:tc:mysql:8.0.36:///vrspfab_test
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

paypal:
  api:
    baseUrl: https://api-m.sandbox.paypal.com
  client:
    id: test-client-id
    secret: test-client-secret

sendpulse:
  api:
    id: test-api-id
    secret: test-api-secret
  from:
    email: test@example.com
    name: SmartFleet Test